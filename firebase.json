{
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "predeploy": [
        "npm --prefix \"$RESOURCE_DIR\" run lint",
        "npm --prefix \"$RESOURCE_DIR\" run build"
      ]
    },
    "emulators": {
      "functions": {
        "port": 5001,
        "host": "localhost"
      },
      "firestore": {
        "port": 8089,
        "host": "localhost"
      },
      "ui": {
        "enabled": true
      }
    }
  ]
}
